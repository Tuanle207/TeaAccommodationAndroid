import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    tittle:{
        fontSize: 20,
        fontWeight: "bold",
        alignSelf: "center",
        color: "#fff",
        marginTop: 15
    }
})

export default styles;